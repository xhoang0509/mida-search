{
    "name": "mida-search",
    "version": "1.0.0",
    "description": "``` mkdir -p ./data/elasticsearch mkdir -p ./data/kibana sudo chmod -R 777 ./data/elasticsearch sudo chmod -R 777 ./data/kibana docker compose up -d ```",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "start": "node src/index.js",
        "dev": "nodemon src/index.js"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "dependencies": {
        "@elastic/elasticsearch": "7.17.0",
        "cors": "^2.8.5",
        "express": "^5.1.0",
        "mongoose": "^8.16.4",
        "stream-chain": "^3.4.0",
        "stream-json": "^1.9.1"
    },
    "devDependencies": {
        "nodemon": "^3.1.10"
    }
}
